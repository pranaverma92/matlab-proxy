import { test, chromium} from '@playwright/test';

test.describe('MATLAB Proxy tests', () => {

});

    // test('Code is running', async ()=> {
    //     let browser = await chromium.launch();
    //     let context = await browser.newContext();
    //     const page = await context.newPage();
    //     await page.goto("http://localhost:41651/index.html");
    //     await page.waitForLoadState('domcontentloaded');
    //     // const textEditorElement = page.locator('.keyCapture');
    //     // const textEditorElement = page.locator('textarea[aria-label="Command Window"]');
    //     // const textEditorElement = page.getByRole('textbox', { name: 'Command Window' });
    //     // const textEditorElement = page.locator('textarea.keyCapture');
    //     page.locator("textarea").fill('ver');    // textEditorElement.press('Enter');
    //     await page.pause();
    //     await page.pause();
    // }))
